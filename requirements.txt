# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
huggingface-hub>=0.20.0
mediapipe>=0.10.9
pywin32>=306
pillow>=10.0.0

# Pick ONE of these depending on your hardware:
# CPU only (works everywhere):
onnxruntime>=1.17.0
# GPU (NVIDIA CUDA - uncomment if you have an NVIDIA GPU):
# onnxruntime-gpu>=1.17.0
